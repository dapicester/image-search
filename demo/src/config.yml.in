categories:
  art:
    dir: ${DATA_DIR}
    type: [color, shape, combined]

  art_abstract:
    dir: ${DATA_DIR}
    type: [color, shape, combined]

  art_animal:
    dir: ${DATA_DIR}
    type: [color, shape, combined]

  art_landscape:
    dir: ${DATA_DIR}
    type: [color, shape, combined]

  art_other:
    dir: ${DATA_DIR}
    type: [color, shape, combined]

  art_portrait:
    dir: ${DATA_DIR}
    type: [color, shape, combined]

  art_still_life:
    dir: ${DATA_DIR}
    type: [color, shape, combined]

  bag:
    dir: ${DATA_DIR}
    type: [color, shape, combined]

  shoe:
    dir: ${DATA_DIR}
    type: [color, shape, combined]

  woman_shoe:
    dir: ${DATA_DIR}
    type: [color, shape, combined]

# vim: set ft=yaml ts=2 sw=2:
